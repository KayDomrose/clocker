## Commands

```bash
clocker init                            Initialize clocker on your machine

clocker servers                         List all configured servers

clocker add                             Configure a new server
clocker start <name>                    Start a server
clocker stop <name>                     Stop a server
clocker deploy <name> <compose-file>    Deploy docker-compose file to server
```
